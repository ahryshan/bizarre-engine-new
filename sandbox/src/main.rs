use std::{thread::sleep, time::Duration};

use bizarre_engine::{
    event::EventQueue,
    window::{Window, WindowCreateInfo, WindowTrait},
};

fn main() {
    // let create_info = WindowCreateInfo::normal_window("Bizarre Window".into(), [600, 400].into());
    //
    // let mut window = Window::new(&create_info).unwrap();
    // window.map().unwrap();
}
